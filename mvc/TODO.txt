- instancje kolekcji sa tworzone w metodzie getCollection implementowanej w kazdej konkretnej klasie DomainObject

	czy to jest poprawne miejsce? Matt Z tworzy je w fabryce

	mechanizm tworzenia powinien byc zdefiniowany w warstwie base, zeby nie trzeba go byle definiowac dla kazdej aplikacji
	powinno sie rowniez zapewnic mozliwosc latwej wymiany tego mechanizmu np poprzez zastosowanie fabryki
	
- obiekty klas z hierarchi mapper (wraz z klasa DAO) sa tworzone i dodawane do rejestru w FrontController

	nie jest to poprawne miejsce, klasa FronController powinna byc ogolna, potrzebna refaktoryzacja

- klasa DataBaseAccess ma zaszyte dane do bazy

	dane do bazy powinny byc umieszczone w pliku konfiguracyjnym

- dodac druga tabele w BD

- dodac mechanizm ktory zapewni brak wielokrotnego tworzenia identycznych obiektow domenowych

- dodac mechanizm ktory zapewni ze obiekty domenowe ktore nie sa zmienione nie beda wstawiane/odczytywane do/z bazy

- dodac join w zapytania do bazy

- wprowadzic przestrzenie nazw i zmodyfikowac system autoload

	w tej chwili autoload jest w pilki index.php, nie jest to poprawne miejsce